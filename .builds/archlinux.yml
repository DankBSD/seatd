image: archlinux
packages:
   - meson
   - linux-headers
   - clang
   - clang-analyzer
   - scdoc
   - valgrind
sources:
   - https://git.sr.ht/~kennylevinsen/seatd
tasks:
   - prepare: |
      meson -Dlogind=auto -Dseatd=enabled -Dbuiltin=enabled -Dexamples=enabled build seatd
   - build: |
      ninja -C build
   - unittest: |
      ninja -C build test
   - smoketest-builtin: |
      timeout -s KILL 30s ./seatd/.builds/smoketest-builtin.sh
